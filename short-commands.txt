git commit -am "new commit"
git checkout -b "branch name"
git log
git log --graph
git branch
git init
git merge --squash "feat-d"
(it depends on the org to use this) 
git log --oneline
git log --graph